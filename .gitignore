# compiled binary (built via `just build`)
/k10s

# coverage artifacts (`just cover`)
coverage.out
coverage.html

# cross-compiled release archives (`just release`)
/dist

# CodeGraph index (rebuild with `codegraph init .`)
/.codegraph

.DS_Store
